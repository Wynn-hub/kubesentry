---
# priorityClassNotSet: Deployment without priorityClassName (audit mode)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: no-priority-class
  namespace: test-builtin-rules
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      automountServiceAccountToken: false
      # priorityClassName NOT set — triggers audit rule
      containers:
      - name: app
        image: nginx:1.19
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
