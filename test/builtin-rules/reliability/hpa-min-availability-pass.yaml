# hpaMinAvailability: HPA with minReplicas >= 2 (audit mode, should PASS)
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: safe-hpa-min
  namespace: test-builtin-rules
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80
