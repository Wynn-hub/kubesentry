# hostIPCSet: Pod without hostIPC (should PASS)
apiVersion: v1
kind: Pod
metadata:
  name: safe-no-host-ipc-pod
  namespace: test-builtin-rules
spec:
  hostIPC: false
  automountServiceAccountToken: false
  containers:
  - name: app
    image: nginx:1.25.0
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: ["ALL"]
