# rolePodExecAttach: Role without pods/exec create permission (should PASS)
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: test-safe-role
  namespace: test-builtin-rules
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch"]
