---
# clusterrolePodExecAttach: ClusterRole with pods/exec create permission
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: test-dangerous-clusterrole
rules:
- apiGroups: [""]
  resources: ["pods/exec"]
  verbs: ["create"]
