apiVersion: v1
kind: Pod
metadata:
  name: memory-request-test
  namespace: test-builtin-rules
spec:
  containers:
  - name: app
    image: nginx:1.19
    resources:
      requests:
        cpu: "100m"
      limits:
        cpu: "500m"
        memory: "256Mi"
