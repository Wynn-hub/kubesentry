apiVersion: v1
kind: Pod
metadata:
  name: cpu-request-test
  namespace: test-builtin-rules
spec:
  containers:
  - name: app
    image: nginx:1.19
    resources:
      limits:
        cpu: "1"
        memory: "128Mi"
